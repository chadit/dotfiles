local M = {}

M.config = function()
  -- local hasModule = pcall(require, "material")
  -- if hasModule then
  local g = vim.g
  g.material_style = "Deep ocean"
  -- g.material_style = "darker"
  -- require('material').set()
  --  require('material').set()
  -- end
end

return M
